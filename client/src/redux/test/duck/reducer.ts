const initialStore = {
    init: 'test',
};

const reducer = (state = initialStore, action: any) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default reducer;
